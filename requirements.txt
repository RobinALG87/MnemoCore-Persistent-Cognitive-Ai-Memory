# NOTE: This file is maintained for legacy compatibility/Docker.
# The canonical source of dependencies is pyproject.toml.
# For development dependencies, see requirements-dev.txt.
# For optional visualization dependencies, see requirements-optional.txt.

# Core
numpy>=1.24
requests>=2.31.0
fastapi>=0.100.0
uvicorn>=0.23.0
pydantic>=2.0.0

# Phase 3.0 - Configuration
pyyaml>=6.0

# Phase 3.5 - Distributed Infrastructure
redis>=2.0.0
qdrant-client>=1.7.0
prometheus-client>=0.17.0
loguru>=0.7.0
msgpack>=1.0.0
mcp>=0.1.0

# Performance
faiss-cpu>=1.7.4

# Reliability
pybreaker>=1.0.0

# CLI
click>=8.1.0
tabulate>=0.9.0

# Subconscious AI Worker (Phase 4.4+)
# Required for all model providers (Ollama, LM Studio, OpenAI, Anthropic)
aiohttp>=3.9.0
# Required for CPU-usage monitoring in ResourceGuard
psutil>=5.9.0

# Async SQLite support (Phase 5.1+)
aiosqlite>=0.19.0

# Cron scheduling (Phase 5.1+)
croniter>=1.3.0

# JSON streaming (Phase 5.1+)
ijson>=3.0.0
